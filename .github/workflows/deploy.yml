name: Build and Deploy to Azure Artifacts

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Configure Maven settings
        env:
          AZURE_DEVOPS_USERNAME: ${{ secrets.AZURE_DEVOPS_USERNAME }}
          AZURE_DEVOPS_PAT: ${{ secrets.AZURE_DEVOPS_PAT }}
        run: |
          mkdir -p ~/.m2
          echo "${{ secrets.MAVEN_SETTINGS }}" | \
          sed "s|\${env.AZURE_DEVOPS_USERNAME}|$AZURE_DEVOPS_USERNAME|g" | \
          sed "s|\${env.AZURE_DEVOPS_PAT}|$AZURE_DEVOPS_PAT|g" > ~/.m2/settings.xml

      - name: Build with Maven
        run: mvn clean install

      - name: Publish WAR to Azure Artifacts
        run: mvn deploy
